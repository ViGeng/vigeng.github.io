# Pinned to compatible-release ranges: patch/minor updates are allowed,
# major bumps (which may break the build) are held back. Bump the upper
# bounds deliberately after testing `mkdocs build --strict` locally.
mkdocs-material>=9.6,<10
mkdocs>=1.6,<2
pymdown-extensions>=10.14,<11
markdown>=3.7,<4
mkdocs-git-revision-date-localized-plugin>=1.3,<2
